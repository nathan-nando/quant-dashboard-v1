const fs = require('fs'); const file = 'c:/code/quant-v1/quant-dashboard-v1/src/app/thresholds/page.tsx'; let content = fs.readFileSync(file, 'utf8'); content = content.replace(/Number\(value\)/g, 'parseValue(value)'); const parseValueCode = '\n  const parseValue = (val: any) => {\n    if (typeof val === \'string\') {\n      const parsed = Number(val.replace(\',\', \'.\'));\n      return isNaN(parsed) ? val : parsed;\n    }\n    return Number(val);\n  };\n'; content = content.replace('const updateConfig =', parseValueCode + '\n  const updateConfig ='); fs.writeFileSync(file, content);
